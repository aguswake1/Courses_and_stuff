const producto = 'Monitor 20 pulgadas';
const precio = "50 U$D";

console.log(producto, " vale ", precio);
console.log(producto + " vale " + precio);
console.log(`${producto} vale ${precio}`); // feature