const producto = "Monitor, 20 pulgadas";
// repetir la cadena de texto, se redondea a 2
const texto = "en promoción".repeat(2.4);

console.log(producto.split(" "));
console.log(producto.split(", "));