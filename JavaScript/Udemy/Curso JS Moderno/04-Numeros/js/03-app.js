let resultado;

// PI
resultado = Math.PI;

// redondear
resultado = Math.round(2.5); // 3
resultado = Math.round(2.4); // 2

// redondear hacia arriba
resultado = Math.ceil(2.3);

// redondear hacia abajo
resultado = Math.floor(2.8);

// raiz cuadrada
resultado = Math.sqrt(81);

// absoluto (módulo)
resultado = Math.abs(-10);

//potencias
resultado = Math.pow(2, 3); // dos al cubo

//minimo
resultado = Math.min(40, 2, 10, -5); // -5

// max
resultado = Math.max(40, 2, 10, -5); // 40

// aleatorio dentro de un rango (30)
resultado = Math.floor(Math.random()*30);