for (let i = 0; i < 10; i++) {
    console.log(`numero ${i}`);
}

// par o impar
for (let i = 0; i < 10; i++) {
    if (i % 2 === 0) {
        console.log(`${i} es par`);
    } else {
        console.log(`${i} es impar`);
    }
}